Read CLAUDE.md to load project conventions and context.
Read DEVELOPMENT_PLAN.md to find the next step that is not yet complete (look for steps without a ✓ mark or "DONE" label).

Implement that step fully:
1. Create every file and folder listed under that step — no placeholders, no TODOs
2. Write production-quality TypeScript — zero `any` types, all props typed
3. Follow every convention listed in CLAUDE.md exactly
4. Use existing utilities and hooks from src/lib/ and src/hooks/ — don't rewrite what exists
5. Handle isLoading and isError states in every data-dependent component
6. After implementing, run through the Checkpoint listed at the bottom of that step

Report back:
- What files were created or modified
- The result of the Checkpoint verification
- What the next step is
